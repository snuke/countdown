{
  "name": "countdown",
  "version": "0.1.0",
  "homepage": "https://snuke.github.io/countdown",
  "private": true,
  "dependencies": {
    "moment": "^2.22.1",
    "moment-duration-format": "^2.2.2",
    "npm": "^6.1.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
